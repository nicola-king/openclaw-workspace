# Artifact Design Profile

- `Markdown`：内容源与版本审阅。
- `HTML`：白底网页报告，可浏览、可归档。
- `Word`：客户月报交付和批注。
- `PDF`：正式归档和不可编辑版本。

版式约束：白底背景；表格 `border-collapse`；所有单元格有边框；长字段使用 `word-break` 和 `overflow-wrap`；PDF 使用 A4 landscape；Word 保留标题、段落和表格结构。
